import { style } from '@vanilla-extract/css';

export const styles = {
  textConteinar: style({
    display: 'flex',
    justifyContent: 'center',
  }),

  text: style({
    width: 'fit-content',
    textAlign: 'left',
  }),
};
